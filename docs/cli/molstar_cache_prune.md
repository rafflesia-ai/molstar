## molstar cache prune

Prune cached remote inputs

```
molstar cache prune [flags]
```

### Options

```
      --cache string        cache directory (default ".molstar-cache")
      --dry-run             print what would be removed
  -h, --help                help for prune
      --json                write JSON report
      --older-than string   only prune entries older than a duration such as 720h
```

### SEE ALSO

* [molstar cache](molstar_cache.md)	 - Inspect and prune the Mol* download cache
