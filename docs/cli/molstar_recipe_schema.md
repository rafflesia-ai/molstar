## molstar recipe schema

Print the JSON Schema for recipe specs

```
molstar recipe schema [flags]
```

### Options

```
  -h, --help         help for schema
      --info         print schema versioning and compatibility metadata
      --json         write JSON errors if the command fails
  -o, --out string   schema output path; use - for stdout (default "-")
```

### SEE ALSO

* [molstar recipe](molstar_recipe.md)	 - Create and compile friendly render recipes
